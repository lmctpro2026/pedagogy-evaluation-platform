# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets — never commit real Supabase keys
.env
.env.*
!.env.example

# Local dev artefacts
node_modules/
dist/
build/
.cache/

# Generated PDF exports (live downloads, not source)
*.pdf

# Local agent / preview-server config (machine-specific)
.claude/

# Supabase CLI cache (machine-specific, may contain tokens)
supabase/.temp/
supabase/.branches/

# Local handover notes — contains live API keys, CLI tokens, admin password
MASTER_CONTEXT.md

# Sprint reports and presentation material (kept local, not published)
PEP_Sprint*_Report*
SPRINT*_*
PRESENTATION_SCRIPT*
DEMO_SCRIPT*
build-slides.js
md2pdf.py
stamp_page_numbers.py
